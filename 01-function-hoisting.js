/* 
Scope -> variables are only accessible some places.
Hoisting -> Code order does not always dictate
“execution order”
3 types of scope : local scope,global scope, block scope
  use "var" when declare variable in local & global scope
  
*/
